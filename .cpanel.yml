deployment:
  tasks:
    - export DEPLOYPATH=/home/bryce603/brycecodes.com/attendance-1/
    - /bin/cp -R * $DEPLOYPATH
  branch: main # or 'main' depending on your default branch name
  remote_host: github.com
  protocol: https